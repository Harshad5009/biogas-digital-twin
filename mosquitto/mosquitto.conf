# Mosquitto MQTT Broker Configuration
# For local development only — adjust for production

listener 1883
allow_anonymous true

# Enable WebSocket listener for browser clients (optional)
listener 9001
protocol websockets
allow_anonymous true

# Logging
log_type all
log_dest stdout
